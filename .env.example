# Server-only OmniRoute AI Gateway credentials.
# Never expose OMNIROUTE_API_KEY to browser/client bundles.
OMNIROUTE_API_KEY=
OMNIROUTE_BASE_URL=http://localhost:20128/v1

# Optional: when set, `pnpm run omniroute:check` also performs a tiny chat completion.
OMNIROUTE_VALIDATION_MODEL=
